# About Me
set realname = "Eric DeMoney"
set from = "edemoney@mail.csuchico.edu"
set use_from = yes

# My credentials

set smtp_url = "smtp://edemoney@mail.csuchico.edu@smtp.gmail.com:587/"
set smtp_pass = "clqllwcuqizaudyr"
set smtp_authenticators = "login"
set imap_user = "edemoney@mail.csuchico.edu"
set imap_pass = "wueelumwvcplweat"

# My mailboxes
set folder = "imaps://imap.gmail.com:993"
set spoolfile = "+INBOX"
set record = "+Sent"
set trash = "+Trash"
set postponed = "+Drafts"
mailboxes =INBOX =Sent =Trash =Drafts =Junk

# Where to put the stuff
set header_cache = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set certificate_file = "~/.mutt/certificates"

# Sidebar
set sidebar_format = "%B%?F? [%F]?%* %?N?%N/?%S"
set mail_check_stats

# Sidebar binds
bind index,pager \CP sidebar-prev                 # Ctrl-n to select next folder
bind index,pager \CN sidebar-next                 # Ctrl-p to select previous folder
bind index,pager \CO sidebar-open                 # Ctrl-o to open selected folder
bind index,pager \CB sidebar-toggle-visible       # Ctrl-b to toggle visibility of the sidebar

# Etc
set sort_aux = reverse-date-received
set mail_check = 30
set move = no
set imap_keepalive = 900
set sort = threads
set editor = "vim"

# prefer plain text
alternative_order text/plain text/enriched text/html
auto_view text/html

# GnuPG bootstrap
# source ~/.mutt/gpg.rc
